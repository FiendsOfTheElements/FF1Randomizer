<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>netcoreapp1.1</TargetFramework>
    <PostBuildEvent>rem "C:\Program Files\7-Zip\7z.exe" -ttar a "$(ProjectDir)$(TargetName).tar" "$(TargetDir)*"
rem "C:\Program Files\7-Zip\7z.exe" -tgzip a "$(ProjectDir)$(TargetName).tar" "$(TargetDir)$(TargetName).tar"</PostBuildEvent>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="RomUtilities" Version="1.4.1" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\FF1Lib\FF1Lib.csproj" />
  </ItemGroup>

</Project>